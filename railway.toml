[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[env]
UV_SYSTEM_PYTHON = "1"
startCommand = "uv run uvicorn nexow_backtesting.api:app --host 0.0.0.0 --port $PORT"
